package main

// Goal of program:
// - The user provides a directory path
// - The tool goes through the directory working out the size of all files
// 		and directories within that directory
// - The tool lists out each subdirectory and its size
// - The user can select a subdirectory and the tool will display the top 10 dir's
//		or files in it by size

func main() {
	// Grab dir parth from command line if no path supplied use current

	// Call Function to retrieve all subdirectories of the given directory
	// For each subdirectory calculate its total size and track the size of individual items

	// Sort subdirectories by size

	// List the top 5 largest items in each Subdirectory (directories or files)
}
